# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your credentials.
# Make sure to include the database name in the path (e.g. /minnesota_db?retryWrites=true...)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/minnesota_db?retryWrites=true&w=majority
